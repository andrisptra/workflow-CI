name: submission-MSML

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_path: {type: str, default: "asap2_preprocessing/train_preprocessed.csv"}
      model_path: {type: str, default: "model.pkl"}
    command: "python modelling.py --data_path {data_path} --model_path {model_path}"
